# Grimmory — self-hosted books library
# Public domain: library.psimaker.org
# (The directory on airbase is /data/library/ — kept that path; the compose
# directory here is named grimmory/ for clarity since "library" is generic.)

MYSQL_ROOT_PASSWORD=                     # generate: openssl rand -base64 24
MYSQL_DATABASE=grimmory
MYSQL_USER=grimmory
MYSQL_PASSWORD=                          # generate: openssl rand -base64 24
