# Vaultwarden — Bitwarden-compatible password manager
# Public domain: bitwarden.psimaker.org
#
# This stack ships with all sensitive settings inlined as compose-level
# environment values (DOMAIN, signup/sends toggles). The few variables that
# DO want runtime override go here. Keep this file even if it's empty so the
# repo shape stays consistent.

# (none required — everything is hard-coded in compose.yml because this stack
# has been stable for years and there is one operator)
