# xbrowsersync — encrypted browser bookmark sync
# Public domain: bookmarks.psimaker.org

# Mongo backing store. The same credentials are used to seed the Mongo root
# account on first start and to authenticate the xbrowsersync API.
DB_NAME=xbrowsersync
DB_USERNAME=xbsuser
DB_PASSWORD=                             # generate: openssl rand -base64 24
