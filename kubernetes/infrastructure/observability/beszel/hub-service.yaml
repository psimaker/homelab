---
apiVersion: v1
kind: Service
metadata:
  name: beszel-hub
  namespace: observability
  labels:
    app.kubernetes.io/name: beszel
    app.kubernetes.io/component: hub
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: beszel
    app.kubernetes.io/component: hub
  ports:
    - name: http
      port: 8090
      targetPort: http
      protocol: TCP
