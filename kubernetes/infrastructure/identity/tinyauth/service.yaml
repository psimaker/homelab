---
apiVersion: v1
kind: Service
metadata:
  name: tinyauth
  namespace: identity
  labels:
    app.kubernetes.io/name: tinyauth
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: tinyauth
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
