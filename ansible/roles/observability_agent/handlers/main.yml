---
- name: Restart promtail
  ansible.builtin.systemd:
    name: promtail
    state: restarted
    daemon_reload: true

- name: Restart beszel-agent
  ansible.builtin.systemd:
    name: beszel-agent
    state: restarted
    daemon_reload: true
